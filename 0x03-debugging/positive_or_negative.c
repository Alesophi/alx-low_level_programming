include "main.h"

/**
 * positive_or_negative -checks for positove of negative number
 *
 * 0: the number to be cheched
 *
 * Return: always 0
 */

void positive_or_negative(int i);
{
	if (i < 0)
		printf("%d is negative", i);
	else
		printf("%d is positive", i);
}
